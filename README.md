## Kivana (pf-desktop)

This repository now contains the cross-platform desktop app under `pf-desktop/` (Tauri + React).

The legacy macOS SwiftUI implementation was archived to `old-macos-app.zip` and removed from the repo working tree.

